# Synapse 現有功能回歸測試清單 (Regression Testing Checklist)

這份文件記錄了目前 Synapse 應用程式已具備的所有核心功能。
在每次完成一個重大實作階段 (Stage) 後，請照著這份清單逐一驗證，確保新加入的程式碼沒有破壞現有功能。

## 1. 側邊欄 (Sidebar) 與 工作區 (Workspace)
- [ ] **展開/收起側邊欄**: 點擊側邊欄右側的箭頭，能平滑收合與展開。
- [ ] **切換工作區**: 點擊上方的 `ModeSelector`，能切換不同的 Workspace，下方清單應正確變更。
- [ ] **工作區排序/顯示隱藏**: 進入 `ModeSelector` 的編輯模式，能拖曳排序 Workspace，並能切換 `System Workspaces` 的顯示/隱藏。
- [ ] **新增自訂工作區**: 點擊新增按鈕，能順利搜出資料夾、挑選圖示與顏色，成功建立新的工作區。
- [ ] **搜尋與排序**: 側邊欄頂部搜尋框能正常過濾檔案，且排序條件 (名稱、修改時間等) 能正確重新排列清單。

## 2. 檔案樹狀清單 (FileTree)
- [ ] **遞迴展開資料夾**: 雙擊或點擊箭頭可正常展開/收起資料夾，且內部檔案清單能順利向後端拉取並顯示。
- [ ] **切換選定檔案**: 單擊文件時，右側 `MainContent` 能正確反應目前選中的檔案。
- [ ] **拖曳搬移檔案 (Drag & Drop)**: 將檔案/資料夾拖拉到另一個資料夾中，能成功搬移且 UI 自動更新。

## 3. ➕ 新增選單 (AddMenu)
- [ ] **呼叫選單**: 在資料夾旁點擊 `+` 號，能正確在指標位置彈出新增選單。
- [ ] **新增 Google Office**: 能正確在目標資料夾內建立 Google Docs, Sheets, Slides, Forms。
- [ ] **Nexus 擴充格式**: 能正確建立 Markdown Notes (`.md`), Nexus Canvas (`.canvas`) 以及 NotebookLM 連接等。
- [ ] **從模板建立 (Template)**: 游標停留在「從模板建立」時能滑出子選單，點選其中的檔案能成功複製一份到當前資料夾。

## 4. 🖱 右鍵選單 (ContextMenu)
- [ ] **重新命名 (Rename)**: 對檔案點擊右鍵選擇重新命名，能順利修改檔名並同步至雲端。
- [ ] **刪除 (Delete)**: 能順利刪除檔案/資料夾 (移至垃圾桶)。
- [ ] **檢視屬性 (Properties)**: 能正確彈出屬性視窗，顯示對應的元資料。
- [ ] **編輯標籤 (Tags)**: 右鍵點擊編輯標籤，能順利新增/刪除標籤藥丸。如果是自訂捷徑，能正常變更目標網址與自訂圖示。

## 5. 主內容區 (MainContent) 
- [ ] **雙畫面分割模式 (Split Mode)**: 點擊右上角的分割按鈕，能切換為左右對開，且兩側各自有獨立的導覽列與選取狀態。
- [ ] **Google Doc/Sheet 預覽器 (WebView)**: 點擊 Google Office 檔案時，MainContent 能成功透過 WebView 載入 Google 編輯畫面，且切換檔案不會造成無謂的瘋狂重新渲染。
- [ ] **Nexus Canvas**: 開啟 `.canvas` 檔案時，能順利載入無限畫布並進行拖曳或連線操作。
- [ ] **Breadcrumb (麵包屑導覽)**: 頂部導覽列顯示正確的當前路徑，點擊可回到上一層。
- [ ] **外部視窗開啟**: 點擊「在新視窗開啟」圖示，能呼叫系統預設瀏覽器開啟該檔案。

## 6. 主題與外觀 (Themes & Aesthetics)
- [ ] **主題切換**: 點擊標題列設定圖示，能自由切換 Light, Dark, Warm, Eva, Miku 等主題，且 UI 立即反應。
- [ ] **編輯器模式**: 切換主題後，Monaco Editor 與 ReactFlow (Canvas/Graph) 應正確同步深淺色模式。
- [ ] **LaTeX 保持白底**: 無論切換至何種深色主題，LaTeX 預覽區域應保持白底黑字。
- [ ] **圖示正確性**: 設定選單中的主題圖示應與目前選取的主題相符。

## 7. 插件系統與邏輯管道 (Plugin System & Logic Pipeline)
- [ ] **插件自動清理**: 手動刪除 `userData/plugins` 中的插件資料夾並重啟 App，該插件應自動從資料庫註銷且節點庫不再顯示。
- [ ] **文本處理節點**: 驗證 `Text Joiner` 能正確合併 A-D 欄位；`JSON Parse/Stringify` 能正確處理物件與字串轉換。
- [ ] **流程控制節點**: 驗證 `If-Branch` 根據布林值正確分流；`Delay` 節點能按指定秒數非同步延遲（不阻塞 UI）。
- [ ] **運算與比較節點**: 驗證 `Compare` 節點的各種運算子 (`==`, `!=`, `<`, `>` 等) 輸出正確布林值。
- [ ] **雙模式座標分離**: 在邏輯模式與展示模式分別移動同一節點，切換模式後節點應各別回到正確位置，互不影響。
- [ ] **預設可見性驗證**: 拖入 `com.synapse.official.logic::text-output` (預設顯示) 與 `arithmetic` (預設隱藏)，切換到展示模式確認顯示狀態與 Manifest 一致。
- [ ] **型別穩定性**: 驗證 `Type Cast` 節點能將輸入強制轉換為指定型別，並觀察輸出埠位的值是否符合預期。

---
> 📝 **驗證紀錄方式**
> 每次 Stage 完成後，全選複製本區塊並建立新的 `Stage X 驗證報告.md` 或直接在此打勾，確認所有功能皆正常運作後打勾，若有破壞，優先修復該損毀的模組。
